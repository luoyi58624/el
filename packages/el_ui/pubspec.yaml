name: el_ui
description: "el ui library."
version: 0.0.5
repository: https://github.com/luoyi58624/el

environment:
  sdk: ^3.10.0
  flutter: ">=3.38.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  el_dart: ^0.0.12
  el_flutter: ^0.0.11
  go_router: ^17.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.10.4
  el_gen: ^0.0.3

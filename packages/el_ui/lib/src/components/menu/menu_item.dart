part of 'index.dart';

class ElMenuItem extends StatelessWidget {
  /// Element UI 菜单
  const ElMenuItem({super.key, required this.title, this.children});

  final String? title;
  final List<ElMenu>? children;

  @override
  Widget build(BuildContext context) {
    return SizedBox();
  }
}
